<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com. google. android. providers.gsf. permission. READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MamaBabyJourney"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC01PNmd-_3bqJ3nH8mDcFQfTykoKjxRFo" />

        <!-- First_Screen -->
        <activity
            android:name=".First_Screen"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Sign_Up -->
        <activity
            android:name=".Create_An_Account_And_Sign_In.Sign_Up"
            android:exported="false" />

        <!-- Sing_in -->
        <activity
            android:name=".Create_An_Account_And_Sign_In.Sing_in"
            android:exported="false"/>

        <!-- Info_Activity -->
        <activity
            android:name=".Create_An_Account_And_Sign_In.Info_Activity"
            android:exported="false"/>

        <!-- Map -->
        <activity
            android:name=".Create_An_Account_And_Sign_In.Map"
            android:exported="false"
            android:label="@string/title_activity_maps" />

        <!-- Font -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>